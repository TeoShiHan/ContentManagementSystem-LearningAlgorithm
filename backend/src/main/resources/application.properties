server.port=8080

# File Storage (local filesystem - no database needed)
app.storage.base-path=./problem-files

# Templates folder (template.<ext> files are copied when creating new files)
app.templates.base-path=../templates

# CORS
app.cors.allowed-origins=http://localhost:4200

# File upload size
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB

# Disable JPA auto-configuration
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration
